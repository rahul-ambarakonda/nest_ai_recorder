{
  "config": {
    "step": {
      "user": {
        "title": "Nest AI Recorder",
        "data": {
          "name": "Name",
          "camera_name": "Camera name",
          "rtsp_url": "go2rtc RTSP URL",
          "mqtt_topic_prefix": "MQTT topic prefix"
        }
      }
    },
    "error": {
      "mqtt_not_configured": "Set up the MQTT integration first (Settings > Devices & services > Add integration > MQTT)."
    },
    "abort": {}
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "mqtt_topic_prefix": "MQTT topic prefix"
        }
      }
    }
  }
}

